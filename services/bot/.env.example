DISCORD_BOT_TOKEN=
# Register slash commands to a specific guild for fast updates during dev
# Leave empty to register globally (takes up to 1 hour to propagate)
GUILD_ID=

# API base URL used by the bot to call backend
# In Docker Compose, use the service name
API_BASE_URL=http://api:4000

